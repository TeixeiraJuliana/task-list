<div class="container-list .m-flex">  
<%= form_with(model: theme) do |form| %>
  <% if theme.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

      <ul>
        <% theme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title_list, style: "display: block" %>
    <%= form.text_field :title_list %>
  </div>

  <div>
    <%= form.label :status_theme, style: "display: block" %>
    <%= form.text_field :status_theme %>
  </div>

  <div>
    <%= form.label :type_list, style: "display: block" %>
    <%= form.text_field :type_list %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
</div>